package com.example.tests;

import java.util.regex.Pattern;
import java.util.concurrent.TimeUnit;
import org.junit.*;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.*;
import org.openqa.selenium.*;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.support.ui.Select;

public class EditAmputationAndWeightBSADesignations416 {
  private WebDriver driver;
  private String baseUrl;
  private boolean acceptNextAlert = true;
  private StringBuffer verificationErrors = new StringBuffer();

  @Before
  public void setUp() throws Exception {
    driver = new FirefoxDriver();
    baseUrl = "https://coms.vacloud.us/";
    driver.manage().timeouts().implicitlyWait(30, TimeUnit.SECONDS);
  }

  @Test
  public void testEditAmputationAndWeightBSADesignations416() throws Exception {
    driver.get(baseUrl + "/index.php");
    driver.findElement(By.id("tool-2019-toolEl")).click();
    driver.findElement(By.id("ext-gen2826")).click();
    driver.findElement(By.id("checkboxfield-2041-inputEl")).click();
    driver.findElement(By.id("checkboxfield-2040-inputEl")).click();
    driver.findElement(By.id("checkboxfield-2032-inputEl")).click();
    driver.findElement(By.id("checkboxfield-2036-inputEl")).click();
    driver.findElement(By.id("checkboxfield-2038-inputEl")).click();
    driver.findElement(By.id("button-2043-btnEl")).click();
    driver.findElement(By.id("button-1005-btnEl")).click();
    driver.findElement(By.id("ext-gen2827")).click();
    driver.findElement(By.id("ext-gen3043")).click();
    driver.findElement(By.cssSelector("li.x-boundlist-item.x-boundlist-item-over")).click();
    driver.findElement(By.id("ext-gen3046")).click();
    driver.findElement(By.cssSelector("li.x-boundlist-item.x-boundlist-item-over")).click();
    driver.findElement(By.id("button-2053-btnEl")).click();
    driver.findElement(By.id("button-1005-btnEl")).click();
  }

  @After
  public void tearDown() throws Exception {
    driver.quit();
    String verificationErrorString = verificationErrors.toString();
    if (!"".equals(verificationErrorString)) {
      fail(verificationErrorString);
    }
  }

  private boolean isElementPresent(By by) {
    try {
      driver.findElement(by);
      return true;
    } catch (NoSuchElementException e) {
      return false;
    }
  }

  private boolean isAlertPresent() {
    try {
      driver.switchTo().alert();
      return true;
    } catch (NoAlertPresentException e) {
      return false;
    }
  }

  private String closeAlertAndGetItsText() {
    try {
      Alert alert = driver.switchTo().alert();
      String alertText = alert.getText();
      if (acceptNextAlert) {
        alert.accept();
      } else {
        alert.dismiss();
      }
      return alertText;
    } finally {
      acceptNextAlert = true;
    }
  }
}
