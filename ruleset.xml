<?xml version="1.0"?>
<ruleset name="DBITPro_COMS_Standard">

 <!--
    The name attribute of the ruleset tag is displayed
    when running PHP_CodeSniffer with the -v command line
    argument. The description tag below is not displayed anywhere
    except in this file, so it can contain information for
    developers who may change this file in the future.
 -->
 <description>DBITPro - Custom Coding Standard for the VA COMS project</description>

<rule ref="Squiz.Debug.JSLint" />
<rule ref="Squiz.Debug.JavaScriptLint" />




<exclude-pattern>*/PHPConsole/*</exclude-pattern>
<exclude-pattern>session.php</exclude-pattern>
<exclude-pattern>indexClean.php</exclude-pattern>
<exclude-pattern>index_Working.php</exclude-pattern>
<exclude-pattern>main.php</exclude-pattern>


 <!-- Include some additional sniffs from the Generic standard -->
 <!-- https://github.com/metashock/Jm_Console/blob/master/build/phpcs.xml -->
 <rule ref="PEAR" >
	<exclude name="Generic.Files.LineEndings" />
	<exclude name="PEAR.ControlStructures.ControlSignature" />
	<exclude name="PEAR.NamingConventions.ValidFunctionName.FunctionNameInvalid" />
	<!-- I prefer the K&R structure rather than the Allman (see http://techportal.inviqa.com/2009/10/12/usphp_code_sniffer/ "Function Sniffs for the PEAR Standard" ) -->
	<include name="Generic.Functions.OpeningFunctionBraceKernighanRitchie" />
	<!-- I like to have the '{' at the same line as the function definition -->
	<exclude name="PEAR.Functions.FunctionDeclaration.BraceOnSameLine" />
 </rule>

 <rule ref="Generic.Files.LineLength">
  <properties>
   <property name="lineLimit" value="150"/>
   <property name="absoluteLineLimit" value="200"/>
  </properties>
 </rule>



 </ruleset>